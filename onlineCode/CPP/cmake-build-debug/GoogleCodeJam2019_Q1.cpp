#include <iostream>
#include<bits/stdc++.h>

using namespace std;
int main() {

    int N,NT;
    int P;
    int skills[N];
    bool toPrint = true;

    std::cin >> NoOfTest;
    int counter =1;
    while (NoOfTest > 0){

        std::cin >> N;

        char str[] = atoi(N);

        cout << str << endl;

        NoOfTest--;
    }
    return 0;
}